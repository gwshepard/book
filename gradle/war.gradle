apply plugin: "war"

bootWar {
    mainClassName = "com.ea2sa.book.BookApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
